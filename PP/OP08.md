# Отчёт P2P — OP08. Приложение Tkinter (таск-трекер)

| Поле | Значение |
|---|---|
| Кого проверял | olegmir59 |
| Почта кабинета | udom1@ya.ru |
| Формат | асинхронная |
| Репозиторий | https://github.com/olegmir59/ZeroCoder_HW_Tkinter |
| Оценка | 5 |

## Задание с урока
Дополнить/изменить таск-трекер: добавлять, удалять и отмечать задачи, оформить интерфейс.

## Что сделано хорошо
- Есть `add_task`, `delete_task`, `mark_task` — как в разборе урока.
- Поле ввода очищается после добавления, выполненная задача красится через `itemconfig`.
- Окно оформлено цветами Tkinter (`CadetBlue`, `aquamarine`).
- Есть второй файл `HW_OP08(+taskcFile).py` — расширение с сохранением задач в файл, это как раз «положить в портфолио».

## Замечания
- Базовый `HW_OP08.py` почти один в один повторяет конспект урока; ценность для портфолио даёт версия с файлом.
- Нет защиты от пустой строки с пробелами (`if task` пропустит `"   "`).

## Вывод
Функционал урока закрыт, есть дополнение с файлом. Оценка 5.
