# Отчёт P2P — DJ02. Шаблоны Django

| Поле | Значение |
|---|---|
| Кого проверял | Дмитрий К. |
| Почта кабинета | ymd@yandex.ru |
| Формат | асинхронная |
| Репозиторий | https://github.com/DmitriK10/Zerocoder_Python/tree/main/dj02 |
| Оценка | 3 |

## Задание
Сайт на Django: общее меню и подвал через `include`, несколько страниц на Bootstrap.

## Что сделано хорошо
- Проект Django (`manage.py`, приложение `main`).
- Две view: главная и `/new/`, данные в шаблон передаются.
- У маршрутов есть `name`.

## Замечания
- В `views.py` только 2 страницы (`index`, `new`). Для типичного DJ02 ожидаются 4 страницы и `include` меню/подвала.
- Нет явного `base.html` с `{% include %}` в просмотренных urls/views; структура проще, чем требует задание.
- В репозитории лежит `db.sqlite3` — лучше в `.gitignore`.

## Вывод
Базовый Django-сайт есть, но объём шаблонов меньше задания. Оценка 3.
